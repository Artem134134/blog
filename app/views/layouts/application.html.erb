<!DOCTYPE html>
<html lang="en" data-bs-theme="dark">
  <head>
    <title>Blog</title>
     <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">   
  </head>

  <body >

  <div class="container mt-2"> 
   
    <nav class="gap-3 d-flex flex-wrap align-items-start justify-content-start">     
      
    <% if user_signed_in? %>    
      <p> &copy; <%= current_user.email %>
        <%= link_to "Sign Out", destroy_user_session_path, class: 'btn btn-outline-secondary btn-sm', data: {'turbo-method': :delete} %>  
      <% else %>
        <%= link_to "Sign In", new_user_session_path, class: 'btn btn-outline-secondary btn-sm' %>
      </p>   
    </nav>
  </div>  
      <% end %>     

    <main class="container mt-3">
      <%= render 'shared/flash' %>

    <%= yield %>
    </main>      
  </body>
</html>
