<!DOCTYPE html>
<html>
  <head>

    <title>All articles</title>

  </head>
      <body>
  
  <nav>
      <%= link_to 'Contact Us!', new_contacts_path(@contacts) %> |
      <%= link_to 'About', about_path(@about) %> |
      <%= link_to 'Terms', terms_path(@terms) %> |
  </nav>
  <br>
      <h2>List of articles</h2> <br>

      <% @articles.each do |article| %>

        <p><%= article.id %></p>
        <p><strong><%= article.title %></strong></p>
        <p><%= article.text %></p>

        <br />
        <nav>
          <%= link_to 'Show', article_path(article) %> |
          <%= link_to 'Edit', edit_article_path(article) %> |
          <%= link_to 'Delete', article_path(article), data: { 'turbo-method': :delete, 'turbo-confirm': 'Are you sure?'} %>
        </nav>
        

      <hr />
      <% end %>

      
      </body> 
</html>